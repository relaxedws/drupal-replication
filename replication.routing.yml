# Remote routing definition
replication.settings_form:
  path: '/admin/config/replication/settings'
  defaults:
    _form: '\Drupal\replication\Form\SettingsForm'
    _title: 'Replication settings'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE
